<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".reminder.drawerReminder">


    <TextView
        android:layout_width="match_parent"
        android:layout_height="15dp"
        android:text="CLICK ON REMINDER TITLE TO SET ALARM"
        android:textColor="#a9a9a9"
        android:id="@+id/reminder_text_top"
        android:textStyle="italic"/>
    <ListView
        android:id="@+id/reminder_LV"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:id="@+id/reminder_empty_view">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/reminder_empty_TV"
            android:padding="16dp"
            android:gravity="center"
            android:layout_centerInParent="true"
            android:text="You have no current reminder/s! Add one by clicking the button below!"/>
    </RelativeLayout>
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/reminder_add_button"
        android:layout_alignParentBottom="true"
        android:layout_centerInParent="true"
        android:backgroundTint="@color/medium_azure"
        android:layout_margin="16dp"
        android:src="@drawable/ic_action_add"
        app:fabSize="normal"

        />
</RelativeLayout>